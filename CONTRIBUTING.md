# 贡献与本地协作

从 [AGENTS](AGENTS.md)、[当前状态](docs/codex/CURRENT_STATUS.md) 和相关任务契约进入。非微小工作围绕一个可验证问题组织；本地任务文件即可，不强制创建远程 Issue。

分支默认使用 `codex/<topic>`。用户要求发布前至少 10 个有实际价值的提交；记录真实反映开发，不回填日期、不凑次数。代码、测试和必要的接口说明按内聚改动组织，数量只是最低条件。目前已形成 10 个开发阶段正式提交；后续保留这一历史，见 [Git 交接](docs/GIT_HANDOFF.md)。

改变比较语义时，同时检查调用方、公开接口、失败信息和数值边界。新测试验证外部可观察结果及真实错误，不只复述实现。具体命令和证据分类见 [VALIDATION](docs/codex/VALIDATION.md)。文档或模板修改做对应检查即可。

当用户要求 GitHub 协作时，可使用本项目 `.github/ISSUE_TEMPLATE/` 和 PR 模板；模板创建不等于已授权向外提交。项目将来作为独立仓库发布时，`.github/` 应位于该仓库根目录。

PR 描述聚焦问题、最终行为、必要接口影响、实际验证和剩余限制；简单改动可省略无关模板项。模型/数据夹具记录来源与生成方法，失败样例保留。上游源码不得无说明地混入本库。

公开前核对 namespace、仓库地址、许可证、可公开夹具和当前 CI；这些事实未确认时明确待办，不编造。参赛资料与开发仓库分离，细节见 [SECURITY](SECURITY.md)。
