# M2：一个真实 MoonBit ML 项目接入

状态：READY_FOR_DISCOVERY；未下载、锁定或运行候选。依赖：M1 的数值数组接口，兼容性探查可以先进行。

## 问题与候选

现有原型只有项目内样例，需要实际第三方调用链证明库能进入真实开发工作流。

优先探查 [MoonXi-net](https://github.com/moonxi-net/moonxi-net) 的 CPU 最小模型，备选 [MbTorch](https://github.com/c-tomioka/mbtorch)。公开 README 的能力描述不是已验证事实。先检查许可证、固定提交、标准工具链兼容性及无需 GPU 的最小调用。若首选要求自改编译器或过多环境修复，记录原因后切换。

## 目标链路

固定的小型输入和权重 → 实际运行第三方 MoonBit 模型/层 → 薄适配器导出数值数组 → 本库与独立预期比较 → 正常结果通过，植入的数值或形状错误失败。

参考值由独立数学算例或另一实现生成；记录生成代码与版本。不能让被测框架生成 expected，再把 identical expected 当 actual 证明正确。与其他框架对照时，要固定轴顺序、dtype 和容差语义。

## 范围与验收

- 只接入一个真实库即可；以 CPU 小模型前向为首选，不做训练效果实验。
- 集成代码与其依赖放示例/独立模块，核心测试不需要下载外部框架。
- 记录源 URL、许可证、确切版本或 commit、必要依赖、运行命令与输出摘要。
- 至少运行一个合理浮点差异通过、一个数值错误失败、一个形状/布局错误失败的案例。
- 在干净的集成依赖目录可按记录复现；固定导出文件回放单列为 FIXTURE_VERIFIED，现场第三方调用才是 INTEGRATION_VERIFIED。
- 不自动向上游发送 Issue/PR 或发布；这不影响本地接入和只读调研。

## 探索与回退

建议第一个候选探查 60 分钟后检查进展；连续两种有依据的修复失败后转备选。保留版本和错误摘要。两个候选都失败时，继续独立的 M1/场景数据工作，明确真实集成未完成并提出下一方案，不伪造成功、不无休止搭环境。

交付：可重跑的集成示例、独立参考生成方式、来源清单、成功和失败报告，以及更新的 CURRENT_STATUS。
