# 决策记录

长期方向变化追加条目；被替代的决定保留并指向新条目。以下决定来源于当前会话，不伪造远程 Issue 或提交 ID。

## DEC-001：保留 JSON 核心并适配 ML 输出

- 日期：2026-09-14；状态：ACCEPTED。
- 决策依据：用户接受“数值数组适配 + 一个真实项目接入”的路径，并指定世界模型和强化学习都作展示。
- 影响：核心默认语义保留，新增适配层和实际调用证据；场景不能只换名称，必须展示能捕获的具体错误。
- 未采用：新做完整世界模型/RL 训练框架，因超出当前回归工具范围。
- 复核：真实集成长期不可用、主办方要求扩大范围或既定技术边界发生变化时重新评估。

## DEC-002：参考值和证据分类独立

- 日期：2026-09-14；状态：ACCEPTED，工程实现约定。
- 决定：核心单元测试、固定夹具、真实第三方调用和远程 CI 分项记录；参考值不由被测实现简单自生成。
- 影响：合成世界模型/RL 例子可以展示用法，但不能代替真实集成或算法效果实验。
- 复核：只要证据对象、来源或后端改变，重新判定范围。

## DEC-003：数值默认语义连续

- 日期：2026-09-14；状态：ACCEPTED，基于现有原型事实。
- 决定：默认保持对称绝对/相对容差、Double 精度说明和大幅度数值保护。PyTorch 兼容模式若有需要，独立命名并测试，不默默改变已有行为。
- 影响：部分大幅度浮点输入也被保守拒绝；M1 文档说明，真实需求出现后再设计精度策略。

## DEC-004：适配项目协作包

- 日期：2026-09-14；状态：ACCEPTED，用户请求。
- 决定：保留源包的任务契约、验证分类、尝试日志和交接结构，重写项目 skill 与领域内容；所有完整文件放在 `json-regress/`，父工作区仅放入口规则。
- 影响：项目目录可独立携带；不增加全局 skill、插件、强制人工审批或远程自动化。源包和哈希说明见 [协作入口](README.md)。

## DEC-005：实际接入使用独立 CPU 模块

- 日期：2026-09-14；状态：IMPLEMENTED。
- MoonXi 原始 CPU 测试与固定前向计算可在标准工具链运行；保持其 MIT 源码原样，以独立 workspace 选择 CPU，避免上游 CUDA 工作区。
- 参考用 Python 标量定义计算；运行核心或回放 RL 场景不需要 Python/第三方框架，完整校验脚本才执行再生成检查。
- 世界模型展示是真实第三方层上的未训练小模型；RL 展示是实际运行的自建环境。二者来源明确，不宣称算法性能。

## DEC-006：署名待定时保留真实开发阶段

- 日期：2026-09-14；用户要求至少 10 次有效提交，并要求稍后给出身份查询命令。
- 不推断姓名或邮箱。在验证完成时实时保留树、说明和验证摘要；这些是本地快照，不是提交。待用户确认署名后正式形成顺序提交，使用实际提交时间。详情见 docs/GIT_HANDOFF.md。


## DEC-007：从原始阶段树建立正式提交

- 日期：2026-09-14；状态：COMPLETED。用户确认 GitHub CLI 登录完成，已核验活动账号为 willzhang。
- 使用该账号公开署名与 GitHub 的 ID 型 noreply 邮箱，仅写入本项目 Git 配置；没有显示或导出令牌原文，也未使用私人邮箱。
- 原始 10 个阶段树按记录顺序形成父子提交，树哈希逐一核验；作者/提交者时间为实际创建时间，原阶段观察时间只保存在说明中。没有重新拆分代码、空提交或回填日期。
- 提交数量检查已通过；本次仅完成本地 Git 工作，未创建远程仓库或发布。详见 [开发历史](../DEVELOPMENT_HISTORY.md)。

## DEC-008：文件协议与可携带比较重放

- 日期：2026-09-15；状态：IMPLEMENTED，用户明确同意评估中的 A/B 两项。
- checking 保持纯 MoonBit；CLI 为独立 Native 模块，官方 x/fs 不进入核心依赖。规则与报告使用 schema 1，文件错误/来源不可比为退出 2，正常回归为退出 1。
- 检查点包以唯一 id 和有序记录验证结构，默认严格核对来源，跨后端/参考生成器差异需显式声明变量。结构不合时先报告，不静默重排。
- 失败包保存原始文本，重新执行比较并核对报告。实际模型/环境重运行是单独的生成器链路，两种证据明确区分。
- 默认数值公式、轨迹连续性与原 Json API 不变。文本边界增加非零下溢检查。验证后进入 M4，不自动扩展训练或第二个框架。
