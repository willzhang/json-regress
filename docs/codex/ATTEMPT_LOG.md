# 尝试记录

用于环境兼容性、数值边界和跨实现差异的重复探索。按需追加，普通文档修改无需写一条。不要删除失败历史；若旧判断被修正，追加结论。

## 已有原型经验（回顾记录，2026-09-14）

| 问题 | 实际观察 | 处理与结论 |
|---|---|---|
| 中文工具链路径 | Moon 工具的环境变量解析崩溃 | 工具链迁到英文临时目录，源码保留原位置；包装脚本封装路径 |
| 极小数下溢 | 标准解析器把 `1e-999` 转成 0 且未保留 repr | Json 接口无法恢复原文；记录限制，不声称完全避免精度损失 |
| 报告排序 | 默认字符串比较先比长度 | 显式 lexical_compare，增加不同长度路径回归用例 |

这些来自原型验证过程，不是本轮重新执行的实验，也不是外部 ML 集成结果。

## 新尝试填写格式

日期 / 任务 / 代码或外部依赖版本：
假设与固定条件：
最小改动或环境修复：
实际命令与结果摘要：
参考值来源、输入及随机性条件（适用时）：
证据类别：
结论：KEEP / REVISE / REJECT / INCONCLUSIVE
失败原因及下一步：

## 2026-09-14：M1–M3 实施

| 观察 / 失败 | 修复与结果 |
|---|---|
| 默认 sandbox 无法解析 github.com | 经沙箱网络授权读取公开源码；未发送个人资料，锁定 MoonXi 提交 |
| 从上游 CPU 子目录运行仍加载根工作区 CUDA 依赖 | 使用官方 `MOON_WORK=off` 验证单 CPU 模块，集成自己的 moon.work 只选择 CPU；不修改编译器 |
| Mooncakes 索引为空，x@0.4.43 无法解析 | 官方 moon update 后下载声明依赖，CPU 115/115 通过 |
| 未限定包的 workspace info/fmt 会修改上游文件并生成 moon.mod | 恢复本轮引入的格式/接口改动，删除本轮生成的影子 manifest；改为仅选择本模块两个包，锁定源码再次校验干净 |
| 第三方 shape 类型未直接导入时无法访问字段 | 通过其公开 Tensor::dims API 获取实际形状，未用期望 shape 覆盖 |
| 本项目旧式 StringBuilder、StringView 和 Float 转换触发弃用警告 | 使用当前工具链建议接口；本项目核心 deny-warn 通过，上游原始警告保留 |
| 步号保留 `1e-999` 原文时原实现会接受被下溢后的 0 | 轨迹步号复用 numeric 输入校验，新增负例验证拒绝；未修改容差 |
| JSON 核心/数值扩展可能因后续改动漂移 | 196 组标量边界对照测试；另验完整计数、默认 8 条样本与 JSON 导出隔离 |

固定输入、权重、独立 Python 算式与参考值见 fixtures，真实库提交见 integrations/moonxi/upstream.json。世界模型出现约 1.19e-8 的 Float 与 Double 算式差异，低于预先设置的 1e-6；未根据错误放宽阈值。完整本地验证与范围见 VALIDATION。

## 2026-09-15：M5 文件与模型重放

| 观察 / 失败 | 修复与验证 |
|---|---|
| 文件中的 `1e-400` 由标准解析器变成零，原始 Json API 无法恢复 | 仅在新增文本入口扫描合法 JSON 的数字 token，避开字符串和转义；非零下溢报错。原核心语义不变。CLI 负例与数值样本测试通过 |
| 显式 max_nesting_depth 参数触发当前工具链弃用诊断 | 使用标准解析器默认 1024 上限，结构检查再限 128；未放宽 deny-warn |
| 新增稳定编码最初使用默认 String 排序，默认按长度比较 | 改用 lexical_compare，补不同长度键的公开编码行为断言 |
| 工作区 info 再次影响上游生成接口 | 恢复本轮产生的 13 个 pkg.generated.mbti 改动；改为 `info . cmd/showcases cmd/export`，fetch 脚本确认锁定源码干净 |
| 收尾发现缺失轨迹行的 location 多一个空键后缀 | 修正 `/3/` 为 `/3` 并增加行为测试，全库双后端各 67 项和 CLI 19 项通过 |
| 跨后端实际模型支持尚不确定 | 原一步模型 Wasm 探测通过，随后真正运行 Native/Wasm 三步生成器，两侧与独立 Python 比较，未以夹具重放冒充模型运行 |

结果：KEEP。完整证据见 VALIDATION；工具链和上游版本不变，阈值预设 1e-6 未调宽。

## 2026-09-15：工具链更新后的测试配置

远程环境更新至 moon 0.1.20260915 / moonc v0.10.13，严格检查指出 examples/rl 的根库测试导入未被直接使用。尝试移除导入后，0.10.12 对传递的 Difference 类型无法访问结构体字段。最终保留导入并显式标注这个类型，使依赖在两版编译器中都明确可见；不关闭警告、不改变断言或比较语义。0.1.1 的交付清理保持有效，0.1.2 同时包含此兼容修复。

同轮远程检查还指出 CLI 未使用 core/json 导入；该命令只调用 checking 的文件文本接口，删除此导入后重新执行 CLI 进程验收。
