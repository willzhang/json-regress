# 资料与运行边界

当前项目是本地开发库。没有在本项目中发现或处理真实报名资料；以下用于约束后续引入的数据，而非声称已经发生泄露。

身份证、银行卡、学历材料、个人联系方式、API 令牌和账户凭据不进入公开仓库、夹具、日志或 Issue。赛事资料保存在项目之外；上传、联系官方或发布按用户实际授权执行。

测试使用自制数据或有适当许可的小型公开数据，记录来源和许可。私有模型权重、训练日志及真实研究数据不能仅因本地可访问就视为可公开。

固定夹具与生成结果分开。外部依赖和下载缓存放 `.external/` 或项目外，生成结果放 `artifacts/`；不为了让测试通过覆盖基准。不要把过大的模型或日志随库分发。

当前 `format_differences` 会包含参与比较的值，本身不做敏感字段脱敏。发布报告前应选用可公开输入；不能把“忽略字段”当作完整的数据访问控制。

本库只为有限、无环的小型 JSON 回归输入设计，尚未验证针对不可信超大输入的资源限制。不要将其部署为公开上传服务后沿用这些原型假设。

发现敏感内容时仅保留定位所需摘要，使用维护者已约定的私密渠道；当前未配置安全联系邮箱，不猜测收件人或自动对外发送内容。
